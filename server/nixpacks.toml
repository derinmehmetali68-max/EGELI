[phases.setup]
nixPkgs = ["nodejs_20", "npm"]

[phases.install]
cmds = ["npm install", "cd ../client && npm install"]

[phases.build]
cmds = ["cd ../client && npm run build", "npm run migrate"]

[start]
cmd = "npm start"

